## 10/08/2026 - 15:00

### Adicionado
- Evento neutro "Operação Normal" (`e6`) em `data/questions.json`, sem
  efeito sobre recursos.
- `Game.core.sortearEvento()`: sorteio ponderado de eventos — o evento
  neutro tem 50% de chance de ocorrência a cada rodada; os demais 5
  eventos dividem os outros 50% uniformemente.
- Fluxo de oferta de venda de recursos: `venda-offer-request` →
  `venda-offer` → `venda-offer-response`, com novo modal
  `modalVendaOferta` para o comprador aceitar ou recusar a oferta antes
  da transação ser efetivada.
- `Game.ui.closeAllModals()`: fecha todos os modais do jogo (evento,
  venda, oferta de venda, assessoria, resultado) ao trocar de tela.

### Corrigido
- **Vendas de recursos não pediam confirmação do comprador**: a
  transferência de KPI/recursos era executada unilateralmente pelo
  vendedor. Agora o comprador precisa aceitar explicitamente a oferta
  (`btnAceitarVendaOferta` / `btnRecusarVendaOferta`) antes que
  `processVenda()` seja chamada.
- **Modal de evento (e demais modais) permanecia visível após "Sair"
  da Sessão/Partida ou "Encerrar" Sessão/Partida**: `showScreen()`
  agora fecha todos os modais a cada troca de tela; `leaveSession()`,
  `endSession()` e o handler de `session-ended` (nos demais clientes)
  fecham os modais explicitamente antes de navegar para fora do jogo.
- Ausência de qualquer chance de "rodada neutra": todas as rodadas
  aplicavam efeito de recursos; introduzido sorteio ponderado com
  evento neutro a 50%.

### Alterado
- `js/game-debug.js`: a simulação automática (`simularPartidaCompleta`)
  passou a usar `Game.core.sortearEvento()` em vez de sorteio uniforme
  direto, mantendo o comportamento simulado fiel ao motor real do jogo.

### Verificado (sem alteração funcional)
- Fluxo de Assessoria envolvendo o host como solicitante e/ou assessor:
  confirmado que `requestAssessoria`, `handleAssessoriaRequest` e o
  auto-despacho de `sendToPlayer` já suportam o host normalmente. Nenhum
  bug de bloqueio foi encontrado; aplicada apenas melhoria de UX (item
  acima) para reduzir ambiguidade.

## 10/08/2026 - 15:15

### Corrigido
- **Rodízio "todos respondem antes de repetir" podia ser quebrado por
  jogadores sem recursos**: em `pickNewPair()` (js/game-core.js), jogadores
  ativos sem recursos eram excluídos de `comRecursos` e, por consequência,
  nunca registrados em `state.usedRespondedorThisRound`. Quando os demais
  jogadores (com recursos) esgotavam suas tentativas no ciclo, o código
  resetava `usedRespondedorThisRound = []` e sorteava de novo, permitindo
  que eles respondessem uma segunda vez antes do jogador sem recursos ter
  sequer sido considerado no ciclo. Agora, jogadores ativos sem recursos
  (fora da rodada de Reserva de Contingência) são marcados como "turno
  usado" (skip) assim que a rodada é avaliada, preservando a garantia de
  que ninguém responde duas vezes antes que todos tenham tido sua vez
  (ou pulado por falta de recursos).
- **README.md desatualizado em relação a `data/questions.json`**: a tabela
  de Eventos e o item de Funcionalidades citavam apenas 5 eventos, mas o
  evento neutro `e6` ("Operação Normal"), introduzido junto com o sorteio
  ponderado (`Game.core.sortearEvento()`), não estava documentado.
  Atualizado README.md para refletir os 6 eventos existentes.

### Alterado
- `todo.md`: item "Garantir que todos os jogadores do jogo respondam ao
  menos uma vez antes que algum jogador responda novamente" marcado como
  concluído, com referência à correção acima.

## 10/08/2026 - 15:20

### Corrigido
- **`game.html` continha marcadores de diff (`+`) não removidos** no bloco
  do modal `#modalVendaOferta`. Por estarem fora das tags, esses `+`
  formavam nós de texto soltos no `<body>` e eram renderizados
  visivelmente na página (o `display:none` da `<div>` do modal não cobre
  texto solto fora dela). Removidos todos os prefixos `+` do bloco,
  mantendo a estrutura HTML original (divs corretamente balanceados).
- **Botão "✕ Cancelar" do modal de Venda de Recurso (`btnFecharVenda`)
  não fazia nada**: o listener em `js/game-ui.js` (`setupUI()`) estava
  comentado. O jogador não conseguia fechar/cancelar o modal de venda
  manualmente, ficando preso até a confirmação/rejeição vinda do host.
  Listener reativado, chamando `Game.ui.fecharVendaModal()`.

### Alterado
- `todo.md`: adicionados os dois itens acima como concluídos.

## 10/08/2026 - 15:30

### Corrigido
- **README.md — tabela "🎯 Funcionalidades" desatualizada quanto ao número de
  eventos**: a correção anterior (10/08/2026 15:15) atualizou apenas a tabela
  da seção "🃏 Eventos" (5 → 6, incluindo o evento neutro `e6`), mas a tabela
  de Funcionalidades continuava afirmando "📋 5 eventos". Corrigido para
  "📋 6 eventos", coerente com `data/questions.json` (6 objetos em `eventos`)
  e com `Game.core.sortearEvento()`.
- **Comentário desatualizado em `js/game-core.js`**: o cabeçalho do arquivo
  ainda documentava "Recursos iniciais: 10 por jogador", divergindo de
  `CONFIG.RECURSOS_INICIAIS` (20). Atualizado o comentário para refletir o
  valor real de configuração.
- **Texto fixo enganoso em `js/game-debug.js`**: a mensagem de ajuda de
  `fakePlayers(3)` no console (F12) exibia "📦10 recursos" fixo, também
  divergente de `CONFIG.RECURSOS_INICIAIS` (20). Alterado para interpolar o
  valor de `CONFIG.RECURSOS_INICIAIS` dinamicamente, evitando nova
  divergência caso o valor de configuração mude no futuro.
- **Placeholder estático em `game.html` (#myRecursos)**: o valor inicial
  fixo no HTML ("10") não refletia `CONFIG.RECURSOS_INICIAIS` (20). Embora
  seja sempre sobrescrito por JS antes da tela de jogo ficar visível (sem
  impacto funcional/visual para o usuário), o valor foi atualizado para "20"
  por consistência com a configuração real do jogo.

### Verificado (sem alteração funcional)
- Demais regras do README (KPI por acerto, valor de venda de recurso,
  duração da sessão de 90min, bônus de Assessoria, timeout de Assessoria,
  restrições de quem pode ser assessor, fórmula de KPI Final, contagem de
  76 perguntas) foram conferidas linha a linha contra o código e estão
  corretas — nenhuma divergência adicional encontrada nesta revisão.

### Alterado
- `todo.md`: adicionados os itens acima como concluídos; item de Assessoria
  para o host atualizado de "pendente" para "verificado — já funcionava".

## 10/08/2026 - 15:45

### Corrigido
- **`css/style.css` não definia diversas classes usadas em `index.html`** (`.screen-choose`,
  `.screen-form`, `.screen-title`, `.screen-desc`, `.choose-buttons`, `.room-id-input-group`,
  `.room-id-prefix`, `.form-input-inline`, `.success-icon`, `.room-id-display`,
  `.room-id-display-label`, `.room-id-display-box`). O bloco "PÁGINA: ENTRY (index.html)" do CSS
  cobria apenas o cabeçalho/logo/rodapé da tela de entrada, deixando as telas "Criar Nova Sala",
  "Sala Criada" e "Entrar em uma Sala" — o primeiro contato do jogador com o app, conforme o fluxo
  descrito no README ("Host cria uma sala..." / "Jogadores entram na sala") — sem estilo algum:
  título/descrição em fonte padrão do navegador, o agrupamento de prefixo (`pm-the-kpi-master-`) +
  input do ID da sala desalinhado, e a caixa de ID da sala criada sem o tema dark/glassmorphism do
  restante do jogo. Adicionado o bloco de regras faltante, reaproveitando as variáveis de design já
  existentes (`--purple`, `--cyan`, `--text-secondary`, `--border-subtle`, `--radius-md`/`--radius-sm`,
  `--font-display`) para manter consistência visual com `game.html`.

### Alterado
- `todo.md`: adicionado o item acima como concluído.

## 10/08/2026 - 18:00

### Corrigido
- **Rodízio de Respondedor podia "reiniciar" após migração de host**: a garantia de que todos os
  jogadores respondem antes que alguém repita dependia de `state.usedRespondedorThisRound`, uma
  lista mantida apenas na memória local de quem era o HOST no momento — nunca sincronizada pela
  rede (não ia em `state-sync`, `round-start` nem `host-changed`). Quando um GUEST assumia como
  host via `Game.network.becomeHost()` (após queda de conexão do host anterior), sua cópia local
  dessa lista era a inicial (vazia), e o rodízio recomeçava do zero sem qualquer aviso — permitindo
  que um jogador fosse sorteado como Respondedor novamente antes dos demais. Esse ponto era o
  suspeito mais provável por trás do relato em `todo.md` ("quando um jogador erra uma resposta
  chamou novamente"), já que esse tipo de coincidência costuma aparecer perto de reconexões/queda
  de host, não por causa de acerto ou erro em si.
- Substituída a lista por um contador de respostas por jogador
  (`state.respostasCount[nome]`, `js/game-state.js`). `Game.core.pickNewPair()` (js/game-core.js)
  passou a sortear o Respondedor sempre entre quem tem o MENOR valor no contador — jogadores sem
  recursos que "pulam a vez" também têm o contador incrementado, para não ficarem permanentemente
  atrás na fila assim que voltarem a ter recursos.
- O contador é transmitido a cada `round-start` (`js/game-core.js` → `js/game-network.js`) e também
  incluído em `state-sync` (entrada/reconexão de jogador) e `host-changed` (migração de host), para
  que qualquer cliente que venha a assumir como host já tenha a contagem correta.
- Removida a recursão de `pickNewPair()` (parâmetro `depth`, guard de `MAX_PLAYERS * 2` e o reset de
  `usedRespondedorThisRound` quando `available.length === 0`): com seleção por contador, sempre há
  pelo menos um candidato elegível na primeira tentativa, então a recursão — que era outro ponto
  frágil, já que resetava a fila inteira no meio de um ciclo — deixou de ser necessária.
- `js/game-core.js` — `nextTurn()`: simplificada para sempre chamar `pickNewPair()`. A distinção
  antiga entre "resetar e chamar `startNewRound()`" vs "chamar `pickNewPair()`" só existia para
  saber quando zerar a lista antiga; sem lista para zerar, as duas chamadas eram idênticas na
  prática. `startNewRound()` permanece usada apenas no início da partida (`startGame()`).
- `js/game-main.js` (`saveState`/`tryRestoreState`) e `js/game-debug.js` (`fakeStartGame`)
  atualizados para persistir/resetar `respostasCount` em vez de `usedRespondedorThisRound`.

### Alterado
- `todo.md`: item de rotação de Respondedor marcado como concluído, com a causa raiz documentada
  (migração de host sem sincronizar o estado de rodízio) para referência futura.

  ## 11/08/2026 - 10:00

### Corrigido
- **Estado local de Assessoria ficava preso em 'pending' após rejeição por
  fase de Encerramento**: em `js/game-ui.js` (`showAssessoriaResult()`), a
  limpeza de `state.currentRound.assessoria` só ocorria para rejeições
  inválidas com `motivo !== 'fase-encerramento'`. Quando o host rejeitava
  um pedido especificamente por essa razão, o registro local otimista
  (setado em `escolherAssessor()`) permanecia com `status: 'pending'` pelo
  restante da rodada. Como `displayQuestion()` deriva o `disabled` das
  alternativas do Respondedor de `round.assessoria?.status === 'pending'`,
  qualquer nova renderização da pergunta na mesma rodada (reconexão, F5,
  ou migração de host) travava as alternativas do Respondedor até o
  timeout de 60s da resposta (RESPOSTA_TIMEOUT) expirar e forçar uma
  resposta automática errada — contrariando a regra do README de que "a
  decisão final é sempre dele [do Respondedor]". Corrigido para limpar
  `state.currentRound.assessoria` em toda rejeição inválida,
  independentemente do motivo; apenas a reabilitação do botão "Pedir
  Assessoria" continua condicionada a `motivo !== 'fase-encerramento'`,
  já que essa restrição específica vale para a rodada inteira.

### Verificado (sem alteração — já corretos)
- Rodízio de Respondedor por contador (`respostasCount`), sincronizado em
  `round-start`/`state-sync`/`host-changed` — íntegro.
- Timeout de segurança de resposta (`RESPOSTA_TIMEOUT` = 60s) e sua
  pausa/retomada durante uma Assessoria pendente — íntegro.
- Aborto de rodada quando Perguntador ou Respondedor desconecta
  involuntariamente (`abortRoundIfParticipant`, chamado a partir de
  `removePlayerByPeerId`) — já implementado e coerente com o README.
- 6 eventos (incluindo o evento neutro `e6`, ~50% de chance) e as 76
  perguntas (16 + 10×6) — contagens batem com o README e com
  `data/questions.json`.
- Fluxo de oferta/aceite de venda de recursos, valores de KPI
  (ACERTO_BASE=10, VALOR_VENDA_RECURSO=10, VALOR_RECURSO_FINAL=5,
  ASSESSORIA_ACERTO=5) e fórmula de KPI Final — conferidos linha a linha
  contra o README, sem divergência.

### Alterado
- `todo.md`: nenhum item novo necessário — todos os itens já estavam
  marcados como concluídos, exceto "Assessoria para o host", que
  permanece intencionalmente pendente.

## 11/08/2026 - 11:00

### Corrigido
- **Partida travava permanentemente após a primeira resposta "normal" (jogador
  com recursos, sem evento de Reserva de Contingência)**: em `js/game-core.js`
  (`handleAnswer()`), sobrou uma chamada `state.usedRespondedorThisRound.push(...)`
  não migrada durante a troca do rodízio por lista (`usedRespondedorThisRound`)
  para rodízio por contador (`state.respostasCount`), feita em 10/08/2026 18:00.
  Como `usedRespondedorThisRound` não existe mais em `Game.state`
  (`js/game-state.js`), a chamada lançava `TypeError: Cannot read properties
  of undefined (reading 'push')` — interrompendo `handleAnswer()` ANTES de
  agendar `nextTurn()`/`endGame()` e antes de `Game.saveState()`. Resultado:
  a rodada ficava presa com `currentRound.respondeu = true` para sempre,
  nenhuma nova rodada era sorteada, e o rodízio "todos respondem antes de
  repetir" (`todo.md`) voltava a ser quebrado, já que o contador do
  respondedor nunca era incrementado nesse caminho. Substituída a chamada
  por `state.respostasCount[respondedorName] = (state.respostasCount[respondedorName] || 0) + 1;`,
  consistente com os demais pontos do arquivo que já usam o contador.

### Alterado
- `todo.md`: reaberto e re-fechado o item "Garantir que todos os jogadores
  do jogo respondam ao menos uma vez antes que algum jogador responda
  novamente", com nota de que a causa raiz desta vez era um resquício
  literal (`.push` em campo removido) da própria migração já documentada
  em 10/08/2026 18:00 — não um novo bug de lógica de rodízio.